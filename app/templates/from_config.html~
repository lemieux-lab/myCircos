{% extends "base.html" %}
{% block style %}
<style>
textarea {
    height: 100px;
    width: 100%;
    box-sizing:border-box;
}
</style>
{% endblock %}

{% block content %}
<div class="alert alert-warning" role="alert">
	<span class="fa fa-lightbulb-o" aria-hidden="true"></span>
  	Learn more on <a href="{{ url_for('about_config') }}" class="alert-link">configuration files</a>
</div>
<form action="/ind" method="post"  enctype="multipart/form-data">
	<!-- personal notes from the user -->
	<div class="panel panel-default">
		<div class="panel-heading">
			<h3 class="panel-title">Description</h3>
 		</div>
  		<div class="panel-body">
    			<textarea class="form-control" rows="3" id="comments" placeholder="Notes on the ploted data"></textarea>
  		</div>
	</div>

	<!-- Information -->
	<div class="panel panel-default">
		<div class="panel-heading">
			<h3 class="panel-title">Information</h3>
 		</div>
  		<div class="panel-body">
			<label for="karyotype">Karyotype</label>
			<input type="text" name="karyotype" id="karyotype">
		</div>
	</div>

	<!-- configuration file -->
	<div class="panel panel-default">
		<div class="panel-heading">
			<h3 class="panel-title">Files</h3>
 		</div>
  		<div class="panel-body">
			<label for="config">.tar.gz or .zip</label><br>
			<input type='file' name="config">
		</div>
	</div>

	<br><br>
	<input class="btn btn-primary" type="submit" value="Generate">
</form>
{% endblock %}
